# Copy to .env for local `netlify dev`. In production, set these in the Netlify
# dashboard under Site settings -> Environment variables (never commit real keys).

# data.cityofnewyork.us -> sign in -> Developer Settings -> app token.
# Without it you share a throttled anonymous pool and hit 429s.
SOCRATA_APP_TOKEN=

# NYC API Developers Portal -> Products -> "Geoclient User" -> subscribe to
# "Geoclient - v2" (v1 is deprecated). Optional — the app falls back to GeoSearch,
# but you lose condo billing-BBL detection, which breaks every condo report.
GEOCLIENT_SUBSCRIPTION_KEY=
